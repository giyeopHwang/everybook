import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const PanelFooterContainer = styled(Box)`
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 1rem;
  height: 60px;
`;
