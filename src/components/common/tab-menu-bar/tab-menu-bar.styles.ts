import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const TabMenuBarContainer = styled(Box)`
  display: flex;
  justify-content: space-between;
  align-items: end;
  padding: 0 1rem;
  height: 30px;
`;
