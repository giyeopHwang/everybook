import styled from '@emotion/styled';
import { Tab, Tabs } from '@mui/material';

export const TabMenuItemsContainer = styled(Tabs)`
  display: flex;
  align-items: center;
`;

export const TabMenuItem = styled(Tab)`
  min-width: 4rem;
  max-width: 4rem;
`;
