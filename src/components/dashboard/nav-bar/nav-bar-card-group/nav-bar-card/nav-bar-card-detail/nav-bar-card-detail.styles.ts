import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const NavBarCardDetailsContainer = styled(Box)`
  display: flex;
  flex-direction: column;
  row-gap: 0.2rem;
`;

export const DetailItem = styled(Box)`
  display: flex;
  justify-content: space-between;
`;

export const TitleGroup = styled(Box)`
  display: flex;
  align-items: center;
  column-gap: 0.3rem;
  font-size: 0.9rem;
`;
