import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const NavBarCardHeaderContainer = styled(Box)`
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
`;

export const TitleGroup = styled(Box)`
  display: flex;
  align-items: center;
  column-gap: 0.3rem;
  font-size: 0.9rem;
`;
