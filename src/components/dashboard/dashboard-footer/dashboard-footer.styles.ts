import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const DashboardFooterContainer = styled(Box)`
  display: flex;
  align-items: center;
  position: fixed;
  bottom: 0;
  padding: 0 1rem;
  width: 100vw;
  height: 30px;
  font-size: 0.8rem;
`;
